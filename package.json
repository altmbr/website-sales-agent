{
  "name": "website-sales-agent",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && http-server public -p 8080 -c-1"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "http-server": "^14.1.1"
  }
}
